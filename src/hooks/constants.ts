// ============================================
// Constants for webtoon scroll animations
// ============================================
export const Z_INDEX_BASE = 1000;
export const TRANSITION_DURATION = 1; // Transition time between cuts in play mode (seconds)
export const DEFAULT_CUT_DURATION = 3; // Default hold duration for each cut (seconds)
export const ANIMATION_START_DELAY = 500; // Delay before starting play mode animation (ms)
export const FIRST_CUT_ANIMATION_DELAY = 50; // Delay for first cut animation trigger (ms)

